using System;
using Microsoft.SPOT;

namespace MaCRo.Communications
{
    class Coder
    {
        public void Send(TransportAddress t)
        {
        }

        public void DataReceived(byte[] buffer, int offset, int length, TransportAddress t)
        {
        }
    }
}
